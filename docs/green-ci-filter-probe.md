# CI path filtering acceptance probe

This temporary documentation change verifies that a Markdown-only pull request
runs path classification, lint and the aggregate CI gate. It will be closed
after recording the observed GitHub Actions jobs.
