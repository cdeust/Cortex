# Results directory

Per-run output goes here as `<run_id>/manifest.json` + `<run_id>/items.jsonl`.
Schema defined in `../manifest.py` (protocol §10).

Files in this directory other than `.gitignore` and `README.md` are not tracked.
